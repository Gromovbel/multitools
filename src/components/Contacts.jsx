import React, { useState } from "react";

const Contacts = () => {
  return (
    <div className="contacts">
      <h1 className="contacts-title">Contacts</h1>
    </div>
  );
};

export { Contacts };
